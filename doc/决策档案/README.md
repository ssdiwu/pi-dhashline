# 决策档案

- [0001 采用零运行时依赖的原生 Pi 扩展](0001-采用零运行时依赖的原生Pi扩展.md) — 运行时只依赖 Node 标准库与 Pi 宿主能力。
- [0002 采用文件级八位标签与单文件文本协议](0002-采用文件级八位标签与单文件文本协议.md) — 公共编辑契约使用 8-hex 文件标签和 `SWAP` / `DEL` / `INS` 文本协议。
- [0003 拒绝硬链接文件写入](0003-拒绝硬链接文件写入.md) — 原子替换与硬链接身份无法同时保证时，以失败关闭为准。
- [0004 公开工具采用两层人类可读投影](0004-公开工具采用两层人类可读投影.md) — 默认显示动作摘要，`Ctrl+O` 展开锚点、编辑动作、diff 与错误指导。
- [0005 write 只允许创建新文本文件](0005-write只允许创建新文本文件.md) — `write` 使用原子仅创建语义，已有文件必须回到 `read` + `edit`。
